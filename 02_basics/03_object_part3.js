//Destructuring the object to get values

const course = {
    courseanme:"Web dev",
    price:"499/-",
    courseInstructor:"Gagan"
}
console.log(course)
const {price: cost} = course//changing the name of a price into cost and printing price 
console.log(cost)

const navbar = () => {
    
}