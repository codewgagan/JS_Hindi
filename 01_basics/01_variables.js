const accountId = 100213 //The const declaration declares block-scoped local variables. The value of a constant can't be changed through reassignment using the assignment operator, but if a constant is an object, its properties can be added, updated, or removed.
let accountEmail = "gagan@gmail.com" //The let declaration declares re-assignable, block-scoped local variables, optionally initializing each to a value.

var accountPassword = "123456" 

accountCity = "Banglore"
accountEmail = "ramesh@gamil.com"
accountPassword = "787324"
let accountState;//undefined 

console.log(accountId);
console.table([accountId, accountEmail, accountCity, accountPassword, accountState])
